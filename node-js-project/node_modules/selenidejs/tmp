const sort = arr => {
  for(let f = 0; f < arr.length; f += 1) {
    for (let s = f; s < arr.length - 1; s += 1) {
      if (arr[s] > arr[s + 1]) {
        const tmp = arr[s + 1];
        arr[s + 1] = arr[s];
        arr[s] = tmp;
      }
    }
  }
  return arr;
};

